{
  "name": "vertrag-oracle-reference",
  "private": true,
  "description": "Drives the reference Dredd implementation so its output can be compared against vertrag's",
  "type": "commonjs",
  "scripts": {
    "compile": "node compile.js"
  },
  "_comment": [
    "Pinned exactly, never with a range. This package IS the expectation the Go",
    "implementation is checked against, so a floating version would silently",
    "redefine 'correct' on some future install. Bumping it is a deliberate act:",
    "raise the version here, run the oracle, and deal with whatever diff appears.",
    "",
    "Every package is the antimatter-studios fork. The upstream Apiary toolchain",
    "is archived and its validator stopped at draft-07, so a document written in",
    "OpenAPI 3.1 could not be validated at all. Measuring vertrag against a mix of",
    "forked and upstream packages measured a toolchain nobody runs."
  ],
  "dependencies": {
    "@antimatter-studios/dredd-transactions": "12.2.1",
    "@antimatter-studios/gavel": "1.0.0",
    "@antimatter-studios/core": "0.2.1"
  }
}